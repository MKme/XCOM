Offline callsign data is generated by `npm run fetch-callsigns`.

- `callsigns.json` (created by the script) contains USA and Canada amateur radio callsigns.
- Raw downloads are placed in `assets/data/raw` and can be deleted to reclaim space after generation.
