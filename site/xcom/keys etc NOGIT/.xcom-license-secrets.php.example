<?php
// Copy this file to `.xcom-license-secrets.php` (same folder as license.php)
// and fill in your store.mkme.org consumer key + secret.
//
// IMPORTANT:
// - Do NOT commit `.xcom-license-secrets.php` to git.
// - Block web access to it via `.htaccess` (see below).

return [
  // From store.mkme.org → WooCommerce → Settings → Advanced → REST API
  'consumerKey' => 'ck_REPLACE_ME',
  'consumerSecret' => 'cs_REPLACE_ME',

  // License Manager for WooCommerce API base (store site)
  'apiBase' => 'https://store.mkme.org/wp-json/lmfwc/v2',

  // WooCommerce product id for XCOM.
  // Required: prevents a license key for another product (e.g. XTOC) from unlocking XCOM.
  // Find it in wp-admin -> Products (edit URL shows `post=####`), then paste the numeric id here.
  'productId' => '',
];
